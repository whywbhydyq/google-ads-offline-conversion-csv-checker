{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ]; then exit 0; fi; git fetch origin main --depth=1 && if [ \"$VERCEL_GIT_COMMIT_SHA\" != \"$(git rev-parse origin/main)\" ]; then exit 0; fi; exit 1",
  "installCommand": "npm ci --no-audit --no-fund"
}
