name: CI Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled automatic CI
        run: echo "Automatic CI is disabled. Do not run build, test, lint, or typecheck in this workflow."
